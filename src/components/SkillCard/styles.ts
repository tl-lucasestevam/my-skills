import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonSkill: {
    padding: 16,
    borderRadius: 4,
    backgroundColor: '#1F1E25',
    alignItems: 'center',
    marginVertical: 10,
  },
  textSkill: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },
});
