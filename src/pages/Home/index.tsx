import React from 'react';
import {Text, View} from 'react-native';
import {styles} from './styles';

export const Home = (): JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>Welcome Lucas</Text>
    </View>
  );
};
